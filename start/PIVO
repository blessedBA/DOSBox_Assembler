
.model tiny
.code
org 100h

Start:			mov ah, 09h
			mov dx, offset HelloStr ; DX = &HelloStr
			int 21h
			mov ax, 4c00h 		; DOS Fn 4ch:exit (AL = 0)
			int 21h

HelloStr		db 'FEED POLTORASHKA NOW!!!', 0dh, 0a\h, '$'

end 			Start